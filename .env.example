# Minimal required environment variables for AI Trip Planner Agent

# Google Cloud Configuration (REQUIRED)
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Optional Google Cloud Configuration (will use defaults if not set)
# GOOGLE_CLOUD_LOCATION=us-central1
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json
GOOGLE_APPLICATION_CREDENTIALS=./gcp-config.json

USE_FIRESTORE=true
FIRESTORE_PROJECT_ID="project-id"
FIRESTORE_CREDENTIALS=".firebase-config from service account json" 
FIRESTORE_TRIPS_COLLECTION=trips  

# Basic API Configuration
DEBUG_MODE=true
API_HOST=0.0.0.0
API_PORT=8000

# Database (uses SQLite by default - no setup required)
# DATABASE_URL=sqlite:///./trip_planner.db
